<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="「知晤晨笺」"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testwxy"
        tools:targetApi="31">
        <activity
            android:name=".feature.comment.CommentDetailActivity"
            android:exported="false" />
        <activity
            android:name=".feature.news.NewsDetailActivity"
            android:exported="false" />
        <activity
            android:name=".feature.personal.PersonalActivity"
            android:exported="false" />
        <activity
            android:name=".feature.home.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.news.BannerDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Testwxy" />
        <activity
            android:name=".feature.login.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".feature.login.LoginActivity"
            android:exported="false" />

    </application>

</manifest>